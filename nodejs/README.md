## Start

To start NodeJS docker simply start docker-compose by executing the following
command in the current folder.


```
docker compose up -d
```

To stop it execute:

```
docker-compose stop
```

