apiVersion: v1
kind: Service
metadata:
  name: mysql-replica-8
  namespace: cdc-lab
  labels:
    app: mysql-replica
    stack: mysql8
spec:
  clusterIP: None
  selector:
    app: mysql-replica
    stack: mysql8
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-replica-nodeport-8
  namespace: cdc-lab
  labels:
    app: mysql-replica
    stack: mysql8
spec:
  type: NodePort
  selector:
    app: mysql-replica
    stack: mysql8
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      nodePort: 30409
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mysql-replica-8
  namespace: cdc-lab
  labels:
    app: mysql-replica
    stack: mysql8
spec:
  serviceName: mysql-replica-8
  replicas: 1
  selector:
    matchLabels:
      app: mysql-replica
      stack: mysql8
  template:
    metadata:
      labels:
        app: mysql-replica
        stack: mysql8
    spec:
      initContainers:
        - name: wait-for-primary
          image: mysql:8.0
          imagePullPolicy: IfNotPresent
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-credentials-8
                  key: MYSQL_ROOT_PASSWORD
          command:
            - sh
            - -c
            - |
              set -e
              echo "Esperando a mysql-primary-8..."
              until mysqladmin ping -h mysql-primary-8 -uroot -p"$MYSQL_ROOT_PASSWORD" --silent; do
                echo "  primary aun no responde, retry en 3s..."
                sleep 3
              done
              echo "Esperando a que el usuario repl exista en el primary..."
              until mysql -h mysql-primary-8 -urepl -prepl -e 'SELECT 1' >/dev/null 2>&1; do
                echo "  usuario repl aun no replicable, retry en 3s..."
                sleep 3
              done
              echo "Primary listo."
      containers:
        - name: mysql
          image: mysql:8.0
          imagePullPolicy: IfNotPresent
          env:
            - name: MYSQL_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-credentials-8
                  key: MYSQL_ROOT_PASSWORD
          ports:
            - name: mysql
              containerPort: 3306
          volumeMounts:
            - name: data
              mountPath: /var/lib/mysql
            - name: config
              mountPath: /etc/mysql/conf.d
            - name: initdb
              mountPath: /docker-entrypoint-initdb.d
            - name: healthcheck
              mountPath: /usr/local/bin/replica-healthcheck.sh
              subPath: healthcheck.sh
          readinessProbe:
            exec:
              command: ["/usr/local/bin/replica-healthcheck.sh"]
            initialDelaySeconds: 30
            periodSeconds: 5
            timeoutSeconds: 10
            failureThreshold: 60
          livenessProbe:
            exec:
              command: ["/usr/local/bin/replica-healthcheck.sh"]
            initialDelaySeconds: 120
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              memory: 256Mi
              cpu: 100m
            limits:
              memory: 512Mi
              cpu: 500m
      volumes:
        - name: config
          configMap:
            name: mysql-replica-config-8
        - name: initdb
          configMap:
            name: mysql-replica-initdb-8
        - name: healthcheck
          configMap:
            name: mysql-replica-healthcheck-8
            defaultMode: 0755
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 1Gi
